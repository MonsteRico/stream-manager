{
  "name": "stream-manager-redux",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:backend": "bun run --filter backend dev",
    "dev:frontend": "bun run --filter frontend dev",
    "dev": "bun run dev:backend && bun run dev:frontend"
  }
}
