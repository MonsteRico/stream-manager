import BorderAnimation from "@/components/overlay/BorderAnimation";

export function SingleCameraPage() {
  return (
    <BorderAnimation>
      <div className="flex flex-col items-center justify-center h-full">
        {/* Transparent center for camera feed */}
      </div>
    </BorderAnimation>
  );
}
